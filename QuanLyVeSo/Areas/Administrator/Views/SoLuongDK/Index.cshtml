@using PagedList;
@using PagedList.Mvc;
@model PagedList.PagedList<QuanLyVeSo.Areas.Administrator.Models.SoLuongDK>
<link href="~/Areas/Administrator/Content/TrangChuCss.css" rel="stylesheet" />

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Administrator/Views/Shared/_Layout.cshtml";
}

<p>
    @Html.ActionLink("Thêm số lượng đăng ký cho đại lý", "ThemMoi")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().DaiLy.TenDaiLy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().NgayDK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().SLDK)
        </th>
        <th>
            Sửa
        </th>
        <th>
            Xóa
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DaiLy.TenDaiLy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayDK)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SLDK)
            </td>
            <td>
                @Html.ActionLink("Sửa", "ChinhSua", new { @_ID = item.ID })
            </td>
            <td>
                @Html.ActionLink("Xóa", "Xoa", new { @_ID = item.ID })
            </td>
        </tr>
    }

</table>
<div id="divBody">
    <br />
    Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)/@Model.PageCount
</div>
<div id="divBody" class="MenuTrang">
    @Html.PagedListPager(Model, page => Url.Action("Index", "SoLuongDK", new { _Page = page }))
</div>
